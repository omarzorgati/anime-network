import { JapaneseTextGenerator } from './japanese-text-generator';

describe('JapaneseTextGenerator', () => {
  it('should create an instance', () => {
    expect(new JapaneseTextGenerator()).toBeTruthy();
  });
});
